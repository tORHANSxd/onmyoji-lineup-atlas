# v0.5.0 功能与发行验证

验证日期：2026-09-12。

## 账号与计算

使用用户明确提供的三份平安志JSON进行本地验证，公开报告仅记录匿名计数，不包含账号名称、UID、库存实例ID或原始JSON。

| 样例 | 角色实例 | 御魂 | 每页60条页数 | 奉为达摩 ID 412 |
|---|---:|---:|---:|---:|
| 1 | 4114 | 1858 | 69 | 299 |
| 2 | 713 | 1154 | 12 | 131 |
| 3 | 2770 | 2008 | 47 | 323 |

三个账号全部实例均可按页遍历，未重复、未遗漏、未识别角色ID为0。每份库存派生一组已有六位置、4+2套装的边界目标，计算器找到满足方案；验证程序直接从原始御魂字段与APK数据独立重算八项属性，再比较目标与结果。此项验证不是游戏内实战，也不代表所有在线阵容码已查询成功。结果见 `inventory-v050.json`，脚本为 `tools/verify-inventory-v050.cjs`。

规则来源、比例单位、觉醒与套装合成、最高属性先后约束、搜索预算和差距分见 `docs/SOUL_CALCULATOR.md`。APK的三个原始资源哈希已与用户提供的APK逐项匹配，仓库只保留必要数据事实。

## 自动化与界面

109项Node测试全部通过，结果见 `node-tests-v050.txt`。覆盖原有解析、桌面IPC、数据保护与官方缓存，并新增查询队列、逐条缓存、暂停续传、失败保存、会话取消、八属性目标、额外二件套、重复实例、最高属性与缺口建议等回归。慢速临时文件写入期间注销后，旧快照不会替换已保存数据库，新会话读取等待写入队列结束。

`tools/verify-features-v050.cjs` 使用真实 Electron、生产页面/preload/Worker/查询队列，以及独立合成账号与服务响应。仅测试进程用虚拟时钟核对查询间隔；发行程序没有登录绕过或取消冷却的开关。

界面验收已通过：启动未认证不读业务资料；155条库中仅3条未解析时自动查询3次、逐条持久化；暂停后继续；同码冷却不覆盖缓存；冷却后单条重新解析；磁盘失败不生成有效缓存；135个实例三页完整显示、搜索412得到33条奉为达摩；推荐6条、计算器展示六位置与八属性、深入计算完成；窄窗口无整页横向溢出；重新登录复用缓存；旧会话慢速导入不能写入新会话。结果见 `features-ui-v050.json`，渲染器错误为0。

已有六成员详情单行、长短阵容码复制、主角图片与官方更新设置往返仍通过。重新解析会停止旧计算，迟到结果不会覆盖新目标；官方资料刷新后也会自动重新核对账号。界面截图使用合成库存，已检查正常与窄窗口的表格、素材列表显示；未公开用户账号画面。

本轮未进行新的手机扫码确认或155条原码的真实在线全量查询。登录协议与Python冻结运行时未修改，真实扫码及两次六位置查询沿用v0.4.0的已完成证据；批量调度与本地保存由合成服务验收，最终包另检查真实二维码获取。

## 安装包

最终 Setup 为 `Onmyoji-Lineup-Atlas-Setup-0.5.0-Windows-x64.exe`，258,932,001字节，文件版本0.5.0、中文安装向导、未签名。SHA-256：`f9691609c1020b53c0633945617705e238a2221ef59bbbedb65ceb35806ade58`。

从最终NSIS安装包提取出的224个文件均与构建产物哈希相同。实际运行提取程序，成功获取网易登录二维码与157个服务器；未认证时9类业务接口全部拒绝，业务资料未载入，合成测试数据库哈希不变；结束后登录模块已退出。结果见 `installer-v050.json` 与 `electron-v050-payload-smoke.json`。

已核对安装包内1,382个条目、32项应用源码/资料、149个运行时文件和7个协议源码哈希，未包含私人账号JSON、测试脚本或工作目录。见 `package-audit.json` 与 `release-v050.json`。

当前机器已有用户安装，本轮没有覆盖安装、重装或卸载它；程序验收使用工作区独立目录和合成配置，验证前后现有安装记录与快捷方式保持不变。安装、重装和卸载流程沿用v0.4.0相同NSIS配置的实测证据，本次包内程序运行不等同重新执行安装向导。
