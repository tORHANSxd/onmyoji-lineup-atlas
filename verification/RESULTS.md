# 验证记录（2026-09-12）

此版本为可运行的Windows预览版。下面区分已验证的程序行为、采集覆盖与外部数据缺口。

## 数据覆盖

| 项目 | 结果 |
|---|---:|
| 用户Excel原码出现次数 | 145 |
| Excel唯一原码 | 110 |
| Excel通过指定服务成功解码 | **0** |
| Excel服务拒绝 | 110，HTTP 400 / 阵容码不是有效的 Base64 |
| 软件本地记录 | 175 |
| 其中唯一阵容码 | 155 |
| 另有成员文字资料的记录 | 26（包含只核实部分成员的方案） |
| 副本用途标签 | 37 |
| 保存来源记录 | 63，其中1个抖音页面访问未成功 |
| 官方分页 | 3页（100 / 100 / 75） |
| 官方动态 / 静态目录 | 275 / 275，ID差集为0 |
| 已确认有双形态 / 无独立觉醒 | 193 / 82 |
| 属性接口与官方无觉醒规则存在冲突 | 15个，均保留原始响应校验与规则来源 |
| 适用形态图片 | 936，全部通过文件核验 |
| 适用形态缺图 / 同角色重复有效图片 | 0 / 0 |
| 原生长方形卡面核验 | **0** |

官方SSR石长姬ID为608，9月9日上线；SP百羽凤凰火ID为607，独立稀有度保存。原生卡面、立绘、头像、书签不混计。没有将404错误页的相同哈希当作两张重复图片。

15个无觉醒角色虽有觉醒属性响应，官方页面模板明确排除N/SP/UR及ID401的觉醒展示。相关非适用图片探测失败保留在资源记录中，不计作需要补齐的觉醒形象。

## 自动化验证

- Node测试：31项通过。覆盖原码保留、错误响应、成员槽位关联、同类型多实例、技能ID顺序、重复御魂拒绝、快照替换/合并/恢复、面板单位、套装与数值、全队实例不重用、受限搜索的未知结果、协战、发布数据隔离等。
- 桌面存储相关测试直接调用生产IPC处理函数，使用隔离临时目录与模拟窗口，实际执行Windows文件写入、覆盖、并发队列和导出；验证外部页面不能读取或写入状态，同页片段导航保持可用。
- Python采集测试：8项通过。覆盖全部分页、重复页、总数不符、未知响应结构、HTML伪图片、404/403/429/网络错误区分、图片原始字节与尺寸保留，以及属性响应和页面觉醒规则。
- 936张当前适用图片均重新进行SHA-256、Pillow完整解码、尺寸与路径范围验证。没有逐张完成肉眼身份复核，视觉类型核验使用小样。
- 用户三份实际平安志导出均通过解析，式神/御魂计数分别为713/1154、2770/2008、4114/1858。测试输出不包含玩家名称、账号ID、御魂ID或原始导出内容。
- 最大样例账号整库检查结果：13条明确缺口、162条待解析或需核对；约4秒。17个人物条件找到单体候选，2个部分分配通过全队不重用校验。**这些部分候选不等于完整阵容已被证实可用。**

测试命令：

```powershell
npm test
python -m unittest discover -s tests -p "test_*.py"
python tools/verify-assets.py
node tools/check-real-matching.cjs
```

## 界面验证

在本地预览实际操作并观察结果：

- 搜索和分类筛选；活动入口得到10条当期活动原码记录。
- 同时选择山兔、季得到2条包含两者的参考方案。
- 阵容详情同时显示五个式神大立绘、五个原生方头像、姓名与要求；阴阳师无形象的缺口保留。
- 石长姬按SSR和中文名检索得到1条，官方ID、两形态和来源能够查看。
- SP过滤得到51条，切换觉醒时明确显示无独立觉醒。
- 合成账号通过文件选择器导入，重载后仍能使用；整库检查完成。
- 输入已保存原码可立即查看攻略成员；修改名称保存，重载后“我保存的”筛选得到同一条记录。
- 备份恢复成功；合成账号及测试保存记录已清理。
- 修复启动加载期间操作筛选产生的异常后，复验无新增控制台错误。
- 1440×940桌面布局及窄预览布局已目视检查，详情没有页面横向溢出。

## 桌面构建

Electron 44.3.0，electron-builder 26.15.3，Windows x64；宿主构建日志内核版本10.0.26100。开发态真实Electron启动已加载175条记录、275式神、5个页面，首页24张卡片，0张破图，0个内置账号。

最终便携EXE已实际启动并正常退出（退出码0）：175条记录、275式神、5个页面，首页24张卡片，0张破图，0个内置账号。程序在临时测试目录启动，未使用正式用户数据目录；完整结果见 `electron-release-smoke.json`。发行包内容清单另见 `package-audit.json`，关键代码与打包前源文件的SHA-256一致。

可执行文件未进行代码签名（Authenticode状态 `NotSigned`）。Release附有SHA-256校验文件。

## 未完成项与外部阻塞

1. 指定解析服务拒绝全部已测Excel短码，没有成功样例或公开后端短码映射，无法完成逐码还原。保留原码，允许今后导入有效结构化响应继续补齐。
2. 原生长方形卡面未核实；多数历史角色的正式服上线日期尚未逐项核对；阴阳师/英杰官方形象尚未收录。
3. 样例不含阴阳师/英杰及当前穿戴关系，无法推断；来源没有给出的技能、觉醒、配速、契灵、战斗增益或替代门槛不能默认满足。
4. 副本覆盖仍有缺口，没有声称包含全部副本的全部阵容，也没有做游戏内实战验证。

项目可以用于本地检索、保存、资料查看和有限库存核对；不能把本版的资料覆盖描述为已完成所有原始验收。
