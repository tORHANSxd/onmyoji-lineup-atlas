# v0.4.0 阵容查询与安装验证

验证日期：2026-09-12。发布文件为 `Onmyoji-Lineup-Atlas-Setup-0.4.0-Windows-x64.exe`。

## 修复结果

- 在桌面应用中接入网易扫码登录、服务器分类与搜索、已有角色筛选、角色选择及文字分享码查询。
- 查询沿用研究任务确认的 MPay → SDK 登录 → 游戏网关流程；回包进入现有 V0–V3 本地解码器。`err`、分享键和角色归属不符合本次请求时拒绝结果。
- 修复 `destroy_entity` 的对象归属判断：释放账号对象不会误报角色断开；销毁当前角色仍会终止查询。
- 每次查询使用本次内存凭据重新连接，查询后关闭连接；退出登录、关闭程序、子进程异常或超时会结束连接并清除状态。
- 成功查询的内容标为官方查询结果；可补齐同原码的已有记录，保留名称、用途和备注。复制仍使用原始分享码。完整内容与二维码保持离线可读。

## 真实登录与查询

实际运行发行用的 PyInstaller 登录模块，由用户在手机上扫码确认；没有使用其他任务的登录凭据。

- 加载 157 个服务器条目，读取到本次账号的 2 个角色。
- 在服务器 15012，连续两次独立连接查询用户指定的 `|TA|d902c7045f350394d41df622c66e398a`。
- 两次均返回 `err=0`、相同分享键和 V3「个突/寮突」内容，共 6 个位置：原始 ID 为 15、583、556、557、372、592。
- 实际回包经 `decodeInput` → `adaptTA` → `validateLineup`；5 个式神的御魂主属性均映射为应用的 2、4、6 号位。协议内部从 0 开始计数的 1、3、5 号位在应用中加 1，不能直接当作界面号位。
- ID 15 尚无经过核实的姓名映射，保留为「阴阳师 / 英杰 15」。不根据阵容常见搭配猜测身份。
- 退出后认证标记、二维码和登录进程均清除。可公开的计数记录见 `ta-live-v040.json`；账号标识、角色名、令牌和原始回包不进入验证附件。

首轮真实查询暴露了账号对象销毁误判，修复并重新构建后完成上述两次成功查询。没有把首轮失败算作通过。

## 自动化与界面检查

- 81 项 Node 测试全部通过，见 `node-tests-v040.txt`。覆盖原有解码、原码关联、合并保留、桌面 IPC、子进程生命周期和退出清理。
- 22 项 Python 测试全部通过，见 `python-ta-tests-v040.txt`。覆盖协议封包、MPay、账号对象销毁、角色归属、失败回包、重连及敏感字段不进入公开状态。
- 浏览器预览实际检查：登录区说明桌面端要求，离线 V3 样例解码为 6 个位置，详情单行展示；浏览器控制台没有报错。测试浏览器页已关闭。
- 安装后的程序通过真实 IPC 启动内置登录模块，加载 157 个条目，其中 154 个具有可连接网关；服务器选择含占位共 158 项，未授权时角色选择禁用，获取二维码按钮可用；随后退出模块。

## 最终安装包实测

测试前未发现该应用的既有安装注册表项、快捷方式或正在运行的进程。仅使用工作区内独立安装目录和合成账号库，未读取个人账号文件。

1. 最终 Setup 在含中文和空格的目录执行实际安装，退出码 0；应用、卸载器、当前用户注册表项及桌面/开始菜单快捷方式均正确，注册表版本为 0.4.0。
2. 已安装的 `app.asar` 和登录模块 EXE 与构建产物哈希一致。
3. 从安装目录启动生产程序，读取到 1 个合成账号、156 条阵容（155 条内置 + 1 条测试记录）、275 个式神和 6 位主角；六成员详情单行、长短码剪贴板读回、12 个主角图片元素和更新设置往返检查通过。
4. 同一 Setup 重复安装成功，合成账号与阵容文件的 SHA-256 保持一致。
5. 实际卸载器成功移除应用、注册表项和两个快捷方式，合成账号库保留；默认 AppData 目录中的独立测试标记也保留。随后仅清理测试标记，未留下测试安装。

记录见 `installer-v040.json` 和 `electron-v040-installed-smoke.json`。验证脚本修复了 Windows PowerShell 5 的 UTF-8 读取及 BOM 写入差异后重跑；最终流程全部通过。原始烟测中 `brokenImages=1` 包含未赋予 `src` 且隐藏的登录二维码占位元素，不代表请求图片失败；单独的主角图片检查为 12 个元素、0 个失败。

中文向导和可选安装目录由配置及安装包版本资源确认，本次未逐页执行向导鼠标操作；安装、重装和卸载使用最终文件的静默入口。

## 包完整性及范围

- `app.asar` 共 1,379 个文件条目，28 项关键应用文件哈希与源码一致。
- 随包登录运行时的 149 个文件与构建目录一致，运行时清单的 7 个协议源文件/配置哈希与源码一致；包含 Python 及第三方依赖许可。
- 包审计没有发现私人输入、测试目录或账号文件，见 `package-audit.json`。
- 安装包 258,916,876 字节，版本 0.4.0，未签名。SHA-256：`2e7658c8f2a88162479f067ba22868ae34e370f9a6b61f294ee6dc903f240738`。

当前仅支持网易账号扫码；目录中的其他渠道分类不表示渠道登录已实现。纯数字 `#TA#` 官方阵容编号需要另行核实官方配置表。其余内置文字分享码和服务器未逐项在线验证。未核实的御魂指标、组合语义、数值单位与战斗计算仍保留原值和待核对标记，不据此宣称全队配置可组成。

关闭旧程序后安装新版本即可继续使用原本地资料。查询时需在新程序中重新扫码；测试授权已经退出。
